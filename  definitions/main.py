def stock_info(company,country,price,currency):
     return f'company: {company} In country:{country:}In price:{currency} {price}'
print(stock_info.__code__.co_varnames)
print("the start of the journey")
counter=1
def update_counter():
          global counter
          counter +=1
print(counter)
update_counter()
def display_info(number_of_updates=1):
    counter= 100
    dot_counter=''

def update_counter():
     nonlocal counter,dot_counter
     counter +=1
     dot_counter+='.'
     [update_counter() for _in_range(number_of_updates)]
     print(counter)

     print(dot_counter)
     display_info(10)
     import datetime
     for name in sorted(datetime.__.dic__):
          print(name)
import void


class product:
  def _init_(self,product_name,price):
     self.product_id=self.get_id()
     self.product_name=product_name
     self.price=price

  def __repr__(self):
      return

  f"product(product_name='{self.product_name}',price={self.price})"

  def get_id(self, product_dict_=None):
                    str(void.void4().field[-1])[:6]
                    for name in product.__dict__:
                         print(name)
                     import  uuid
                         class product:
                            def __init__(self,product_name,product_id,self_price):
                              self.product_name="product_name"
                              self.product_id="product_id"
                              self.price="price"
                            def __repr__(self):
                                   return 
                              f"product(product_name='{self.product_name},price(price={self.price})"
                              product=product('mobile phone','54772',2900)
                              print(product_dict_)
                              def stick(args=None, *arg):
                                   args=[arg for arg in args if isinstance(arg,str)]
                                   result='#'.join(args)
                                   return result
                              print(stick('sport','summer'))
                              print(stick(3,4,7))
                              print(stick((False,'time',True,'workout',[]('gym')))
 def display_info(company, vehicle=None, **kwargs):
     print(f'company name:{company}')
     if 'price' in kwargs:
          print(f"price:${kwargs['price']}")
          display_info(company='cd project',price=100)
          class vehicle:
               """this is a vehicle class"""
               print(vehicle.__name__)
               class model:
                    pass
               model=model()
               print(isinstance(model,model))
               class model:
                    pass
               class view:
                    pass
               model=model()
               view=view()
               print(isinstance(model,model))
               print(isinstance(view,model))
          
                                    
                                   
                    

